Theming is set up to be a default loaded 'base' theme directory
and any app specific modifications to the base theme are added by:
- creating a new directory under 'theme'
= adding any overwritten styles/mixins keeping a similar structure if possible
- importing the new 'custom theme' after the 'base' theme

think layers